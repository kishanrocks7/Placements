int height(struct Node* node){
        // code here 
         if(node==NULL)
         return 0;
        int x=height(node->left);
        int y=height(node->right);
        return (max(x,y)+1);
    
    }
