int maxDepth(TreeNode* root) {
        if(root==NULL)
        {
            return 0;
        }
        int maxi=max(maxDepth(root->right)+1,maxDepth(root->left)+1);
        return maxi;
        
    }
